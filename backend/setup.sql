-- Criar o banco de dados
CREATE DATABASE tarefas_db;

-- Conectar ao banco (via psql: \c tarefas_db)
-- O Sequelize cria a tabela automaticamente via sync()
-- Execute este script apenas para criar o banco inicialmente.
